export type Book = {
  id: number;
  title: string;
  author: string;
  price: number;
  quantity: number;
};

export type NewBook = {
  title: string;
  author: string;
  price: number;
  quantity: number;
};
